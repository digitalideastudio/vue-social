import AvatarMedals from './main.vue';

/* istanbul ignore next */
AvatarMedals.install = function AvatarMedalsInstall(Vue) {
    Vue.component(AvatarMedals.name, AvatarMedals);
};

export default AvatarMedals;
