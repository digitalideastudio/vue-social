import SchedulePicker from './main.vue';

/* istanbul ignore next */
SchedulePicker.install = function SchedulePickerInstall(Vue) {
    Vue.component(v.name, SchedulePicker);
};

export default SchedulePicker;
